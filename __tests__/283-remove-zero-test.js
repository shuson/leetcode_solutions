jest.dontMock('../solutions/283-remove-zero');

describe('remove 0 from array and put to behind', function () {
    var relocate0 = require('../solutions/283-remove-zero');

    xit('returns itself when no 0 array', function () {

    });
});
